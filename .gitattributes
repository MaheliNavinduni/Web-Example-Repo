# Normalise line endings so Windows, macOS and Linux teammates do not see
# whole files as "changed" just because of CRLF vs LF.
* text=auto eol=lf

# Binary assets must never be line-ending converted.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.woff binary
*.woff2 binary
